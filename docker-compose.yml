version: '3'
services:
  odoo:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - GIT_TOKEN=${GIT_TOKEN}
    volumes:
      - odoo-web-data:/var/lib/odoo
      - ./.log:/var/log/odoo
    ports:
      - "8069:8069"
      - "8071:8071"
    depends_on:
      - db
    # command: odoo
    command: pytest -s /mnt/extra-addons/stock_account_fifo_fix_valuation -v --disable-warnings

    environment:
      - LOG_ODOO=/var/log/odoo
  db:
    image: postgres:9.6
    environment:
      - POSTGRES_PASSWORD=odoo
      - POSTGRES_USER=odoo
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      - odoo-db-data:/var/lib/postgresql/data/pgdata
    expose:
      - 5432

volumes:
  odoo-web-data:
  odoo-db-data:
