<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_partner_form_delivery_contact" model="ir.ui.view">
        <field name="name">res.partner.form.delivery.contact</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <field name="property_delivery_carrier_id" position="after">
                <field name="delivery_contact_id"
                       domain="[('parent_id', '=', id)]"
                       context="{'default_parent_id': id, 'default_type': 'contact'}"/>
            </field>
        </field>
    </record>

    <record id="view_res_partner_filter_delivery_contact" model="ir.ui.view">
        <field name="name">res.partner.filter.delivery.contact</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">
            <filter name="inactive" position="before">
                <separator/>
                <filter name="delivery_contact"
                        string="Delivery Contacts"
                        domain="[('delivery_contact_for_partner_ids', '!=', False)]"/>
            </filter>
        </field>
    </record>

</odoo>
